class Intime {
  String? intime, outtime, minutes, seconds, devicetoken;
  Intime(
      {this.intime,
      this.outtime,
      this.minutes,
      this.seconds,
      this.devicetoken});
}

Intime userTiming = Intime();
